# 任务：可缩放裁剪框

**输入**：`specs/003-resizable-crop-box/` 下的规格、计划、调研、数据模型和公开契约

**前置条件**：`spec.md` 与 `plan.md` 已确认

**测试要求**：遵循项目章程，新行为必须先补最小且直接的回归测试，再实现对应代码。

**组织方式**：任务按用户故事分组；每个任务包含明确文件路径和依赖顺序。

## 格式：`[ID] [P?] [Story] 描述`

- **[P]**：可与同阶段其他任务并行，涉及不同文件且无未完成依赖
- **[Story]**：对应 `spec.md` 中的用户故事

## Phase 1：基础类型

**目标**：建立所有用户故事共用的内部缩放方向和几何类型，不改变运行时行为。

- [x] T001 在 `lib/interface.ts` 定义八方向缩放、裁剪框几何和缩放会话所需的内部类型

**检查点**：后续测试和组合式函数可以共享同一套类型定义。

---

## Phase 2：用户故事 1——按需缩放裁剪框（P1）🎯 MVP

**目标**：通过默认关闭的公开参数启用四边和四角缩放，并支持鼠标和单指触摸。

**独立测试**：关闭参数时没有控制点；开启后八个方向均可操作，对边固定，鼠标和单指操作不会移动图片或整个裁剪框。

### 测试

- [x] T002 [US1] 在 `tests/useCropResize.test.ts` 添加八方向起始快照几何、固定对边、鼠标会话和单指会话的失败测试
- [x] T003 [US1] 在 `tests/vue-cropper.test.ts` 添加 `cropBoxResizable` 默认关闭、开启后渲染八个控制点的失败契约测试
- [x] T004 [US1] 在 `tests/useCropResize.test.ts` 添加控制点起始事件与裁剪框移动、图片拖拽事件隔离的失败测试

### 实现

- [x] T005 [US1] 在 `lib/composables/useCropResize.ts` 实现基于起始快照的八方向几何计算、鼠标/单指缩放会话和监听清理
- [x] T006 [US1] 在 `lib/vue-cropper.vue` 增加默认关闭的 `cropBoxResizable` prop，接入缩放会话并按条件渲染八个控制点
- [x] T007 [US1] 在 `lib/styles/index.scss` 增加八方向控制点的定位、光标、层级和触摸命中区样式

**检查点**：用户故事 1 可独立演示；默认模式保持不变，开启后可完成八方向自由缩放。

---

## Phase 3：用户故事 2——保持边界和图片覆盖关系（P2）

**目标**：裁剪框始终位于容器内，缩放期间不触发图片回弹，结束后按现有 `centerBox` 规则回弹一次。

**独立测试**：把八个方向拖向容器外均不越界；关闭覆盖模式时图片不变；开启后只在松手时按照现有时长恢复覆盖。

### 测试

- [x] T008 [US2] 在 `tests/useCropResize.test.ts` 添加八方向容器夹取、内部 `24px` 最小交互尺寸和控制点不穿越对边的失败测试
- [x] T009 [US2] 在 `tests/vue-cropper.test.ts` 添加缩放期间跳过回弹、结束时执行一次 `centerBox` 回弹、关闭覆盖模式不改变图片的失败测试

### 实现

- [x] T010 [US2] 在 `lib/composables/useCropResize.ts` 接入容器边界和内部固定最小交互尺寸计算
- [x] T011 [US2] 在 `lib/composables/useCropperWatchers.ts` 增加缩放会话期间刷新裁剪框但跳过图片回弹的内部状态分支
- [x] T012 [US2] 在 `lib/vue-cropper.vue` 接入缩放开始时取消待执行回弹、结束时清理会话并调用一次现有 `reboundImg()`

**检查点**：用户故事 1 和 2 同时成立，边界与 `centerBox` 行为通过回归测试。

---

## Phase 4：用户故事 3——同步裁剪结果并可重置（P3）

**目标**：缩放结果进入现有状态、预览、导出和原图坐标链路，重置恢复最新传入的初始裁剪框。

**独立测试**：缩放后读取 `change`、原图坐标和导出范围均与可见裁剪框一致；调用 `reset()` 后恢复初始尺寸和居中位置。

### 测试

- [x] T013 [US3] 在 `tests/vue-cropper.test.ts` 添加缩放过程持续触发 `change` 和 `real-time`、缩放后原图坐标一致及 `reset()` 恢复初始状态的失败测试
- [x] T014 [US3] 在 `tests/vue-cropper.test.ts` 添加组件提交缩放几何后 `getCropData()` 使用最新裁剪尺寸和位置的失败集成测试

### 实现

- [x] T015 [US3] 在 `lib/vue-cropper.vue` 将一次缩放几何提交同时写入内部尺寸和位置，并调用现有状态与实时预览队列

**检查点**：三个用户故事全部可独立验证，公开输出与当前可见裁剪框一致。

---

## Phase 5：文档、Demo 与完整验证

**目标**：同步公开契约和可操作示例，并完成项目要求的全部检查。

- [x] T016 [P] 在 `src/pages/Props.md` 增加 `cropBoxResizable` 的中英文参数表、默认值、手势和范围说明
- [x] T017 [P] 在 `src/pages/DemoAll.vue` 增加“允许缩放裁剪框”开关并将参数传入现有大画布示例
- [x] T018 在 `tests/vue-cropper.test.ts` 运行并补齐默认关闭时现有移动、缩放、导出和重置行为不变的最小兼容回归
- [x] T019 按 `specs/003-resizable-crop-box/quickstart.md` 执行鼠标与触摸场景检查，并运行 `pnpm run check`、`pnpm run build:docs`、`git diff --check`

---

## 依赖与执行顺序

### 阶段依赖

- **Phase 1**：无依赖，首先完成。
- **Phase 2 / US1**：依赖 T001；T002-T004 先失败，随后依次完成 T005-T007。
- **Phase 3 / US2**：依赖 US1；T008、T009 先失败，随后完成 T010-T012。
- **Phase 4 / US3**：依赖 US1 和 US2 的最终几何；T013、T014 先失败，随后完成 T015。
- **Phase 5**：依赖全部用户故事；T016 与 T017 可并行，T018、T019 顺序收口。

### 用户故事依赖

- **US1**：交付默认关闭的八方向缩放 MVP。
- **US2**：复用 US1 的会话和几何，增加容器边界与回弹时机。
- **US3**：复用 US1/US2 的最终几何，接入现有输出和重置链路。

### 可并行机会

- T002 与 T003 涉及不同测试文件，可并行编写。
- T008 与 T009 涉及不同测试关注点，可在 US1 完成后并行编写。
- T016 与 T017 涉及不同文档页面，可在运行时代码稳定后并行完成。

## 实施策略

### MVP 优先

1. 完成 T001。
2. 先完成失败测试 T002-T004，再完成实现 T005-T007。
3. 验证默认关闭兼容和八方向鼠标/单指缩放。
4. 再进入边界、回弹和结果同步。

### 增量交付

1. **US1**：公开开关、控制点、自由缩放。
2. **US2**：容器边界、最小有效几何、`centerBox` 回弹。
3. **US3**：状态、坐标、导出、重置。
4. **收口**：中英文文档、全功能 Demo、完整检查。

## 备注

- 所有任务只实现已确认规格，不加入比例锁定、可配置最小尺寸、键盘微调或新事件。
- 测试任务必须在对应实现任务前执行并确认失败原因来自尚未实现的目标行为。
- 完成任务后在本文件把对应复选框标记为 `[x]`。

---

## Phase 6：用户故事 1 增量——九宫格与系统裁剪器视觉（P1）

**目标**：开启裁剪框缩放时显示三等分九宫格，并在不改变八方向命中范围的前提下优化边框和控制区视觉。

**独立测试**：开启参数后出现 4 条参考线和 8 个方向控制区；关闭参数、图片未加载或全屏裁剪模式下均不出现；八方向缩放测试继续通过。

### 测试

- [x] T020 [US1] 在 `tests/vue-cropper.test.ts` 添加九宫格与八方向控制区随 `cropBoxResizable` 和裁剪框可见状态共同显示或隐藏的失败契约测试

### 实现

- [x] T021 [US1] 在 `lib/vue-cropper.vue` 将四条九宫格参考线接入现有可缩放裁剪框视觉层，且不参与指针事件
- [x] T022 [US1] 在 `lib/styles/index.scss` 将裁剪框视觉改为三等分参考线、四角 L 形控制提示和四边短线提示，并保留八个 `24px` 命中区
- [x] T023 [US1] 在 `src/pages/Home.vue` 增加裁剪框缩放开关，将首页体验区默认状态设为开启并传入裁剪器

**检查点**：首页首次打开即可看到九宫格和新控制区视觉；组件公开默认值保持 `false`。

---

## Phase 7：文档审查与完整验证

**目标**：所有主要中英文入口准确说明可选缩放和九宫格，并完成视觉与回归验证。

- [x] T024 [P] 在 `README.md` 和 `src/pages/Guide.md` 更新中英文能力、示例和当前范围，移除仅支持固定裁剪框的过时描述
- [x] T025 [P] 在 `src/pages/Props.md` 和 `src/pages/Methods.md` 补充九宫格、首页演示默认值及旧版生命周期方法与当前缩放能力的区别
- [x] T026 按 `specs/003-resizable-crop-box/quickstart.md` 验证首页、全功能 Demo、开关、九宫格、八方向交互和文档，并运行 `pnpm run check`、`pnpm run build:docs`、`git diff --check`

### 增量依赖

- T020 必须先于 T021、T022，并确认失败来自尚未渲染九宫格。
- T021 与 T022 完成后执行 T023；T024 与 T025 可并行。
- T026 依赖 T020-T025 全部完成。

---

## Phase 8：视觉问题修正

**目标**：恢复连续外层边框，使控制提示贴合边界，并将九宫格改为缩放期间显示、松手后渐隐。

- [x] T027 [US1] 在 `tests/vue-cropper.test.ts` 添加连续边框存在、九宫格激活状态随缩放会话切换的失败测试
- [x] T028 [US1] 在 `lib/vue-cropper.vue` 增加独立边框视觉层，并用 `cropResizing` 控制九宫格激活状态
- [x] T029 [US1] 在 `lib/styles/index.scss` 将四角控制提示放在框外并贴合角点、将四边提示贴线，增加九宫格约 `400ms` 渐隐过渡
- [x] T030 [P] 在 `README.md`、`src/pages/Home.vue`、`src/pages/Guide.md` 和 `src/pages/Props.md` 同步九宫格仅在缩放时显示的中英文说明
- [x] T031 按 `specs/003-resizable-crop-box/quickstart.md` 浏览器验证边框、控制器贴边和九宫格渐隐，并运行 `pnpm run check`、`pnpm run build:docs`、`git diff --check`
