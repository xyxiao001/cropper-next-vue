# 文档站重构验收记录

日期：2026-09-21。分支：`codex/main-dev-20260921-2`。

## 实现范围

统一首页、阅读布局、三个场景与工作台的暖白/深绿视觉；增加概念图示、用途分组导航、双语章节链接、移动目录折叠、相关主题与上下篇。保留全部 20 个原有路由，新增 `/concepts`。参考文档两种语言均保留 30 个参数、16 个实例方法、5 个事件名，并补充 loading / drag 插槽说明。

新增 13 份独立 SFC 示例，以原始文件展示和复制。站内演示仍使用原有裁剪组件、图片与导出流程。修正 centerWrapper 覆盖语义、original 像素换算、getCropData 返回类型说明。

## 自动检查

- `pnpm run typecheck`：通过。
- `pnpm exec vitest run tests/docs-navigation.test.ts tests/docs-interactions.test.ts tests/docs-examples.test.ts`：3 个文件、20 项测试通过。
- `pnpm run build:docs`：通过；仍有 Element Plus vendor chunk 超过 500 kB 的构建提示，无构建错误。
- `git diff --check`：通过。
- 未运行 lint；未修改 `lib/`、包版本、依赖或锁文件；未提交、推送、打 tag 或发布。

## 浏览器验证

使用真实开发站 `http://127.0.0.1:5174/#/`，不是设计预览 HTML。

- 375 / 768 / 1440px：20 个文档路由和首页均检查正文容器宽度；无整页横向溢出，表格与代码使用局部滚动。
- 检查了桌面首页、快速开始、概念图、头像场景、工作台的实际画面，以及手机首页、概念、工作台和目录。
- 英文遍历全部文档路由，标题和导航正常；中文原入口正常。
- 快速开始章节链接 `#/guide#export-result` 可定位；切换语言后保留主题与章节。桌面定位约 y=103px，手机约 y=166px，均在页头下方。
- 手机导航展开、选中页面后收起；本页目录可用 Enter 展开，章节链接可用 Enter 跳转。
- 完整代码区可用 Enter 展开、复制按钮可用键盘激活，浏览器显示已复制；单元测试断言完整原文写入 Clipboard API。
- 工作台 Rotate right 后数据面板报告 rotate=90，源图坐标同步；Export Blob 生成真实图片并显示显示尺寸、导出像素和文件大小。
- 英文头像保存、封面应用、商品图保存均产生真实结果；商品图切换中文后也可保存。
- 中文首页旋转后导出得到实际 432×294 图片，导出对话框可关闭。
- 修复检查中发现的概念图类名碰撞、手机工作台工具栏被裁切、桌面工作台画布高度与控件主题覆盖问题。
- 临时视口已恢复，浏览器停在新版首页。

## 独立代码复现

在仓库外临时 Vue + Vite 页面加载复制的 BasicCrop、AvatarCrop、CoverCrop、ProductCrop；仅通过 `vue`、`cropper-next-vue` 包导出和公开样式路径加载，未使用文档站私有 helper 或组件。测试图片为临时本地图片，无上传服务。该验证复用本地包产物，不代表重新从 npm 安装的网络验证。

最终代码的实际导出结果：

| 示例 | Blob | 图片像素 |
| --- | --- | --- |
| BasicCrop | 成功 | 780 × 432 |
| AvatarCrop | 成功 | 432 × 432 |
| CoverCrop | 成功 | 768 × 432 |
| ProductCrop | 成功 | 432 × 432 |

像素值取决于验证时容器和设备像素比，不是示例对所有设备的固定尺寸承诺。另有 13 份示例的 SFC 编译检查和公开依赖检查。

## 规格收敛

已核对 16 项 FR、6 项可由工程验证的 SC、4 个用户故事、计划中的布局/内容/导航决策和 5 项章程原则。首次收敛发现的 T015（移动目录与样式细节）、T016（最终集成与浏览器验证）已完成；无剩余可实施差距。

SC-001、SC-002 是 3 位真实读者的 30 秒认知和 5 分钟接入测试，尚未开展，保留待验收。浏览器与自动化测试不替代真实读者结果。

## 2026-09-21 逐页体验追加验收

根据用户追加要求，完成真实页面、控件、业务场景与响应式体验复查，并修正所发现的问题。详见 [experience-audit.md](./experience-audit.md)，包括每页操作证据及真实触屏、原生文件拖放、下载落盘尚未验证的边界。本轮原文档测试 20 项及新增封面响应式预览测试 1 项均通过。
